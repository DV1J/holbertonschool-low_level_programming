#include <stdio.h>

/**
 *main - prints single digits numbers of base 10 form 0
 *
 *Return: Always 0
 */

int main(void)
{
	int di;

	for (di = 0; di < 10; di++)
	{
		printf("%d", di);
	}

	printf("\n");

	return (0);
}
